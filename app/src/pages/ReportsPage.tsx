import React from 'react';
import PagePlaceholder from '../components/PagePlaceholder.js';

export default function ReportsPage() {
  return (
    <PagePlaceholder
      icon="📊"
      title="Reports"
      description="Project-level governance reports and dashboards including fit-classification distribution, customisation-risk heatmap, Clean Core review queue, and evidence-confidence breakdown. All metrics traceable to underlying records."
      phase="Phase 11 — Dashboards, Audit, and Reporting"
    />
  );
}
